import 'package:flutter/material.dart';

class DisplayTrendingItems extends StatelessWidget {
  const DisplayTrendingItems({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
